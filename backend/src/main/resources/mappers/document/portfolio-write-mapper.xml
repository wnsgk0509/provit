<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.provit.mapper.document.PortfolioWriteMapper">

    <select id="selectNextPortfolioNum" resultType="int">
        SELECT SEQ_T_PORTFOLIO.NEXTVAL
        FROM DUAL
    </select>

    <insert id="insertPortfolio" parameterType="com.provit.dto.user.PortfolioDTO">
        INSERT INTO T_PORTFOLIO (
            PORTFOLIO_NUM,
            USER_NUM,
            PORTFOLIO_TITLE,
            FILE_URL,
            CREATED_AT,
            UPDATED_AT
        ) VALUES (
            #{portfolioNum},
            #{userNum},
            #{portfolioTitle},
            #{fileUrl},
            SYSDATE,
            SYSDATE
        )
    </insert>

    <select id="selectPortfolio" resultType="com.provit.dto.user.PortfolioDTO">
        SELECT PORTFOLIO_NUM, USER_NUM, PORTFOLIO_TITLE, FILE_URL, CREATED_AT, UPDATED_AT
        FROM T_PORTFOLIO
        WHERE PORTFOLIO_NUM = #{portfolioNum}
          AND USER_NUM = #{userNum}
    </select>

</mapper>
