<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.provit.mapper.interview.ResumeMapper">

    <sql id="resumeColumns">
        r.RESUME_NUM,
        r.USER_NUM,
        r.HIGHEST_LEVEL,
        r.EDUCATION_CODE,
        ec.EDUCATION_NAME,
        r.MOTIVATION,
        r.DESIRED_LOCATION,
        r.DESIRED_WORK_TYPE,
        r.CREATED_AT,
        r.UPDATED_AT
    </sql>

    <select id="selectResumeListByUserNum"
            parameterType="int"
            resultType="com.provit.dto.user.ResumeDTO">
        SELECT
            <include refid="resumeColumns" />
        FROM T_RESUME r
        JOIN T_EDUCODE ec
          ON r.EDUCATION_CODE = ec.EDUCATION_CODE
        WHERE r.USER_NUM = #{userNum}
        ORDER BY r.UPDATED_AT DESC, r.RESUME_NUM DESC
    </select>

    <select id="selectResumeByResumeNumAndUserNum"
            parameterType="map"
            resultType="com.provit.dto.user.ResumeDTO">
        SELECT
            <include refid="resumeColumns" />
        FROM T_RESUME r
        JOIN T_EDUCODE ec
          ON r.EDUCATION_CODE = ec.EDUCATION_CODE
        WHERE r.RESUME_NUM = #{number}
          AND r.USER_NUM = #{userNum}
    </select>

</mapper>
