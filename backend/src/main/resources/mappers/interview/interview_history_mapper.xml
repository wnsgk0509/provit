<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.provit.mapper.interview.InterviewHistoryMapper">

    <select id="selectNextHistoryNum" resultType="int">
        SELECT SEQ_T_INTERVIEW_HISTORY.NEXTVAL
        FROM DUAL
    </select>

    <insert id="insertInterviewHistory"
            parameterType="com.provit.dto.interview.InterviewHistoryDTO">
        INSERT INTO T_INTERVIEW_HISTORY (
            HISTORY_NUM,
            USER_NUM,
            QUESTION1,
            ANSWER1,
            QUESTION2,
            ANSWER2,
            QUESTION3,
            ANSWER3,
            QUESTION4,
            ANSWER4,
            QUESTION5,
            ANSWER5,
            INTERVIEW_DATE
        ) VALUES (
            #{historyNum},
            #{userNum},
            #{question1},
            #{answer1},
            #{question2},
            #{answer2},
            #{question3},
            #{answer3},
            #{question4},
            #{answer4},
            #{question5},
            #{answer5},
            NVL(#{interviewDate, jdbcType=DATE}, SYSDATE)
        )
    </insert>

    <select id="selectInterviewHistory"
            parameterType="map"
            resultType="com.provit.dto.interview.InterviewHistoryDTO">
        SELECT
            HISTORY_NUM,
            USER_NUM,
            QUESTION1,
            ANSWER1,
            QUESTION2,
            ANSWER2,
            QUESTION3,
            ANSWER3,
            QUESTION4,
            ANSWER4,
            QUESTION5,
            ANSWER5,
            INTERVIEW_DATE
        FROM T_INTERVIEW_HISTORY
        WHERE HISTORY_NUM = #{number}
          AND USER_NUM = #{userNum}
    </select>

</mapper>
